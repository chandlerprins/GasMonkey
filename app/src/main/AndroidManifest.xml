<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- For Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /> <!-- Location (older Android devices may need it for BLE scanning) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- If you use Bluetooth admin or other older APIs -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_gm_logo"
        android:label="GasMonkey"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_gm_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Airmonitorble"
        android:usesCleartextTraffic="true">

        <!-- Dashboard Screen -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/Theme.Airmonitorble" />

        <!-- MainActivity Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="MainActivity"
            android:theme="@style/Theme.Airmonitorble" />

        <!-- Device Search Screen -->
        <activity
            android:name=".SearchDeviceActivity"
            android:exported="false"
            android:label="Search Device"
            android:theme="@style/Theme.Airmonitorble" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="History"
            android:theme="@style/Theme.Airmonitorble" />

        <!-- Register Screen -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Register"
            android:theme="@style/Theme.Airmonitorble" />

        <!-- Login Screen (MAIN ENTRY POINT) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Login"
            android:theme="@style/Theme.Airmonitorble">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SensorDataActivity" />
        <activity android:name=".ReportProblemActivity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".LogoutActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".WelcomeActivity" />

    </application>
</manifest>